Config file required
====================

Make sure you add a file named `config_private.h` to this directory which looks like:

```config_private.h

#ifndef CONFIG_PRIVATE_H
#define CONFIG_PRIVATE_H

constexpr char NETWORK_SSID[] = "<YOUR SSID>";
constexpr char NETWORK_PASSWORD[] = "<YOUR SSID PASSWORD>";

constexpr char THINGSBOARD_HOST[] = "<host of thingsboard>>";
constexpr char THINGSBOARD_TOKEN[] = "<thingsboard device token>";

#endif // CONFIG_PRIVATE_H
```
